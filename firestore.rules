rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Each person can only read and write their own data.
    match /users/{uid}/data/{app} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
